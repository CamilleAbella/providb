{
  "name": "providb",
  "author": "ghom",
  "license": "ISC",
  "version": "0.0.1",
  "description": "Versatile database provider written in TypeScript.",
  "main": "dist/index.js",
  "types": "dist",
  "prettier": {
    "semi": false
  },
  "scripts": {
    "format": "prettier src test.js jest.config.js package.json readme.md tsconfig.json --write",
    "build": "del-cli dist/** -d && tsc",
    "prepublish": "npm run prettier && npm run test",
    "test": "npm run build && jest"
  },
  "keywords": [],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/CamilleAbella/providb.git"
  },
  "devDependencies": {
    "@types/node": "^14.0.27",
    "del-cli": "^3.0.1",
    "jest": "^26.4.2",
    "prettier": "^2.0.5",
    "typescript": "^4.0.2"
  },
  "dependencies": {}
}
